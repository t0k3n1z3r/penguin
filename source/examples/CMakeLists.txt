
PROJECT(Examples)

SET(PF_EXAMPLES_SRC PfMain.c)
ADD_DEFINITIONS(-DPF_DEBUG_CLASS_DEFAULT=PF_DEBUG_CLASS_PENGUIN)

# Compiler options
ADD_DEFINITIONS(-Wall)

INCLUDE_DIRECTORIES(${PF_PROJECT_DIR}/include)

ADD_EXECUTABLE(penguin ${PF_EXAMPLES_SRC})

TARGET_LINK_LIBRARIES(penguin core)